language: swift
xcode_project: BodyBuilder.xcodeproj # path to your xcodeproj folder
xcode_scheme: BodyBuilder

script: 
- xcodebuild -scheme BodyBuilder -project BodyBuilder.xcodeproj -sdk iphonesimulator -destination 'platform=iOS Simulator,name=iPhone 6S,OS=9.1' build test

after_success:
  - bash <(curl -s https://codecov.io/bash) -J 'BodyBuilder'
  
